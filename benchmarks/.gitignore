# Everything a benchmark run produces or downloads stays out of the tree.
# Harness source, suite definitions, and small deterministic fixtures are the
# only versioned benchmark material.
data/
runs/
results/
artifacts/
.corpus-cache/
.clio-coder-scicode/

# Python bytecode and virtualenvs for the community adapters.
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
