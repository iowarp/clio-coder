# Ephemeral benchmark run outputs and caches (see results/README.md for the
# tracked-vs-untracked convention). .clio-benchmark/ is also ignored repo-wide.
.corpus-cache/
.clio-benchmark/
.clio-scicode/
