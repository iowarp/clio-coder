/**
 * Placeholder for worker-side IPC back to the orchestrator.
 * Will carry provider requests + tool results in P7+.
 * Kept empty to reserve the boundary — imports nothing.
 */
export const PROVIDER_BRIDGE_VERSION = 1;
