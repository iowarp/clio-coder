node_modules/
dist/
*.log
.DS_Store
.env
.env.local
coverage/
.claude/
.clio/
.clio-benchmark/
# Soak fixtures are seeded repositories, and a fixture's command registry is
# fixture source: a repo with no commands.yaml fails a code step by contract,
# so an ignored registry would make the fixture untestable.
!benchmarks/soak/fixtures/*/.clio/
!benchmarks/soak/fixtures/*/.clio/**
.codex
.codex/
.antigravitycli
.tsbuildinfo
__pycache__/

# Orchestrator-of-orchestrators runtime scripts (not part of the product).
scripts/orch/

# Local session handoff notes (not shipped; regenerated per session).




# Dev-time scratch area: planning files, debugging notes, sprint plans, reports. Never shipped.
.superpowers/
benchmarks/battletest
CLIO.md
CLAUDE.md
AGENTS.md



