# Historical Configure Tree Proposal

Historical design proposal for configure and `/settings` information architecture.
Worker models are configured via `fleet.profiles` and resolved through role bindings.
