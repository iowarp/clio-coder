# v0.3.9 sprint progress

One line per merged brief from `.superpowers/prompts/v039/`, in merge order. Commit hashes are on the `v0.3.9` branch.

| Brief | Issue | Commit | Deferred or noted |
| --- | --- | --- | --- |
| 02 eval tracked metrics | #252 (#156 slice) | `7b7b53b8` | EvalArtifact v4 kept; the v1 verdict envelope composes through an explicit Suite v2 adapter. Baseline suite `benchmarks/eval/tracked-metrics-baseline.yaml`, ten code-graded tasks, about 18 minutes per trial on mini; the pane's `--trials 1` run scored 9/10 with the failure recorded as `infrastructure_failure`. The sprint's eval baseline runs from a snapshot of this tree at its pre-rebase base (`fa92cf68` plus this brief) so no wave-2 code is in it. |
| 07 memory spend | #229 | `3cb1ab52` | Bank connected to `records.json` rather than removed. Measured hit rate 6/60 model-tier steps (10.0 percent) at 22,868 tokens per injection; defaults unchanged, timeout 180 s to 30 s. On a shared single endpoint the model tier records `endpoint_busy` at every boundary rather than evicting the resident model, which is the trade #229 asked for. Merge interaction with #253 resolved: the `prewarm` and `background-memory` cost labels coexist. |
| 05 session pre-warm | #253 | `b42704ff` | Measured on mini: resumed first turn 14,171 ms cold to 690 ms hot. This llama.cpp build ignores cancellation mid-prefill (an aborted pre-warm held the slot 88 s), so a submit detaches the round instead of aborting; `ABORT_ROUND_ON_SUBMIT` carries the finding. The endpoint-slot seam was wired to #250's `registerForegroundStream` at merge. A pre-warm nobody uses within mini's 60 s sleep window is wasted; that is a server setting, not a Clio one. |
| 06 endpoint capacity | #250 | `89940b1c` | Live on mini (`--parallel 1`): a three-worker dispatch during the orchestrator's turn is refused with the endpoint denial. Merge interaction with #247 resolved by moving the URL normalizer to `src/core/endpoint-key.ts`; `residencyTargetKey` now returns the same canonical URL dispatch capacity uses, without a runtime prefix. The pre-warm slot registration seam (`registerForegroundStream`) waits for brief 05. |
| 04 prefix layout | #249 | `e415e363` | Measured on mini: a changed context window re-prefills 516 tokens instead of 2,778 (0.72 s instead of 2.64 s). Merge interaction with #247 resolved: `prompt_recompiled` stamps only when the previous hash exists and differs, because a resumed process now names the manifest's last hash as `previousHash`. |
| 01 cache telemetry | #247 | `cb1a9d38` | LM Studio 2.29.0 reports no `timings` on its OpenAI-compatible port, so the `lmstudio-timings` source is accepted but never observed; on LM Studio the verdict stays on pi-ai's `cached_tokens`. Review fix before merge: `prompt_recompiled` stamps only on a genuine hash change, not on a process's first compile. Rebase onto #227 required widening the cold-reason test fixture's window to hold its attested 5,000-token prompt. |
| 03 context reconcile | #227 | `0b54887a` | Criterion 3 (resume carries the loaded window) is proven by contract test only; mini reports a probe window (`--ctx-size 262144 --parallel 1`) and never a loaded one, so no live row on this target can show it. |

## Deviations from the charter

- The operator's `feat(workbench)` commit (2053d42d) landed on `v0.3.9` mid-sprint and is v0.4.0 scope. It was moved to branch `feat/workbench` with its own worktree, and `v0.3.9` was rebased without it before any push, so brief hashes after brief 06 changed once. The Codex pane working on the workbench continues on that branch.
- `/tmp` on the build host is a 4 GB tmpfs and was 95 percent full with the 0.3.8 release-test scratch trees (`clio-v038-*-prefix`, `clio-v038-fixed-source`, `clio-v037-upgrade-prefix`, 2.4 GB, none open by any process). Every full test run under that condition failed with `ENOSPC` inside CLI smoke tests; the four trees were removed on 2026-08-29 before brief 01's verification run.

- The charter's baseline step 1 asks for the eval suite brief 02 names, run three times by hand before brief 02 lands. No suite file exists in the tree at `7e1e20ae` (only run artifacts under `benchmarks/internal/runs/`), so there is nothing to run by hand. The interactive-session and fleet measurements of the baseline are taken as soon as wave 1 merges. The eval-suite measurement is taken after brief 02 merges and before briefs 04, 05, 06, and 07 merge; brief 02 is measurement machinery and moves no prefill number, so the baseline it produces is still a "before" for every brief that does.
- The operator's default orchestrator target `dynamo` is LM Studio on a Windows machine (192.168.86.143:1234), a wrapper over llama.cpp that is far less configurable. The operator directed the sprint to stay on native llama.cpp, so the baseline, the after, and every live check run against `mini` (192.168.86.141:8080, build `b226-2115b73d8`, model `qwen3.8-27b-dense`) with `--target mini`. LM Studio is checked only for the `timings` shape in brief 01.
