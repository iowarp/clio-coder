# v0.3.7 progress

One line per merged brief: commit, what landed, what was deferred. Spec: the
"Clio Idea Map" design artifact. Branch: `v0.3.7`, cut from `main` at
`9b54219d`.

## Wave 1

| Brief | Pane | Commit | Deferred |
| --- | --- | --- | --- |
| dispatch typed intent + host-run verification (#155) | sol | 63e5af5c | Verification executes on singular, parallel, sequential, pipeline, and detached runs; review and compete refuse checks with `verification_unsupported_for_mode`. #158, #159, #163 remain open as follow-ons. The full suite cannot run inside the Codex sandbox (network unshared); the integrator runs `npm run ci`. |
| `/btw` + OSC notify (#204, refs #41) | opus | cbcc356a | `clio-coder usage report` does not yet label side-question calls; `terminal.notify` has no Settings Center row (yaml only); `docs/config-knobs-audit.md` left untouched because it is a frozen historical inventory. |
| single-writer token + worktree per task (#207) | sol | 30d6699e | Readers in a wave finish before the next writer starts (serialized, not maximally overlapped); the cross-process lease refusal fires at dispatch time rather than admission; `writers` is an ExecutionPlan input, not yet a fleet contract field (wave 2, v5). |
| `/handoff` (#206) | opus | 46ec77a5 | The review overlay is read-only plus `e` for `$EDITOR` rather than an in-overlay editor; `clio-coder usage report` does not label handoff rounds. |
| fleet authoring CLI (#209) | sol | c2b6058e | |
| collapse `fleet.ts` onto `executeFleetRun` (refs #208, #209) | sol | a95d5ca9 | `executeFleetRun` now owns the durable fleet-run record and `planFleetResume`; TUI-started runs are resumable from the CLI. |
| `/fleet run` approval preview (#208) | opus | c2693c73 | `src/cli/fleet.ts` still runs its own copy of the scheduler loop; it must collapse onto `executeFleetRun` when the fleet authoring CLI merges. Promoting the run path to a `DispatchContract` method is deferred until a second caller needs it. |

## Wave 2

| Brief | Pane | Commit | Deferred |
| --- | --- | --- | --- |
| council contract (#212) | sol | 2e096c01 | Members on local and HTTP targets only; SSH fleet nodes refused at admission. The council grid and `/council` are the next opus brief. Integrator review required four fixes before merge: integrity bump to v18, error only on a final-round failure, a coordinator-only synthesis receipt with no member provenance, and the missing remote-node test. |
| council surface + `/council` (#213) | opus | 874b5a4a | An operator-typed council carries agent `requestOrigin` because the dispatch tool sets none, so its rows render with the agent glyph; fixing that needs a tool argument. The council card always shows the bounded tail and ignores the board's per-row detail toggle. Columns sort by label because the board row carries no roster index. |
| fleet contract v5 (plan, gate, per-step target) (#214) | sol | af4877ca | Proposal runs and the gate baseline are not ExecutionPlan steps, so they are outside the plan hash; the preview names them on the step. Resume reruns a plan step instead of reconstructing its dynamic tasks. Dispatch-tool exposure of dynamic plans stays a later wave. Integrator review required a rework before merge: the first cut ran delegated tasks in a private loop beside the scheduler, bypassing admission, the writer token, and boundary enforcement. |
| oracle recipe + watchdog (#210) | opus | f9cc8939 | No evidence-domain turn diff exists; the watchdog coalesces per-call `edit`/`write`/`artifact` diffs. `oracle` is excluded from the compiled prompt roster (operator-only shadow). No Settings Center row for `watchdog`. |
| interactive deferral sweep (#211, refs #41 #204 #206 #210) | opus | 66f1ee78 | Per-model side-question and handoff columns stay off the `usage report` model table because they would change the existing table's column set. `retry.streamStallMs` still has no Settings Center row. |

## Wave 3

| Brief | Pane | Commit | Deferred |
| --- | --- | --- | --- |
| resource library (#215) | sol | merged; hash recorded in the next row update | Installed pins live in their own `<configDir>/library-pins.yaml` rather than the skills index pin metadata. Requirements are resolved and reported, installed only with `--with-requirements`. Skills Hub kind tabs are the next opus brief. Integrator review required a follow-up before merge: `sync` with `library.sync: false` reported success, installed requirements were reported as unresolved, the `library` remote name was undocumented, and the brief's tests were missing; the integrator replaced `library use skill` (delegated to a `skills use` command that does not exist) with the installed path and the `/skills` hint. |

## Decisions made during implementation

- #155 reconciliation: the model-facing request gains one `intent` object
  (`read_roots`, `write_roots`, `relevant_paths`, `expected_outputs`,
  `verification: [{check, timeout_ms?}]`). `gate: "<check>"` is the only
  shorthand and expands to one verification entry. There is no separate
  top-level `verify` field. A `check` is a declared id resolved through the
  verify tool's `DeclaredCheck` projection at admission; model text never
  becomes argv. Verification executes on singular, parallel, sequential,
  pipeline, and detached runs in this cut; review and compete accept the
  intent shape but refuse verification entries with a named reason.
- Receipt integrity version moves 15 to 16 with the `verification` field.
- Receipt integrity version moves 17 to 18 with the `council` field (#212) and 18 to 19 with the `fleetGate` field (#214). Every covered-field addition bumps the version.
- Out-of-turn usage (`/btw`, `/handoff`) is persisted to `<stateDir>/usage/out-of-turn.jsonl`, never to the session JSONL, because `/btw` promises it appends nothing to the session file. `usage report` folds that store; the session ledger stays untouched.
