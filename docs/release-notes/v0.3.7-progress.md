# v0.3.7 progress

One line per merged brief: commit, what landed, what was deferred. Spec: the
"Clio Idea Map" design artifact. Branch: `v0.3.7`, cut from `main` at
`9b54219d`.

## Wave 1

| Brief | Pane | Commit | Deferred |
| --- | --- | --- | --- |
| dispatch typed intent + host-run verification (#155) | sol | 63e5af5c | Verification executes on singular, parallel, sequential, pipeline, and detached runs; review and compete refuse checks with `verification_unsupported_for_mode`. #158, #159, #163 remain open as follow-ons. The full suite cannot run inside the Codex sandbox (network unshared); the integrator runs `npm run ci`. |
| `/btw` + OSC notify (#204, refs #41) | opus | cbcc356a | `clio-coder usage report` does not yet label side-question calls; `terminal.notify` has no Settings Center row (yaml only); `docs/config-knobs-audit.md` left untouched because it is a frozen historical inventory. |
| single-writer token + worktree per task (#207) | sol | 30d6699e | Readers in a wave finish before the next writer starts (serialized, not maximally overlapped); the cross-process lease refusal fires at dispatch time rather than admission; `writers` is an ExecutionPlan input, not yet a fleet contract field (wave 2, v5). |
| `/handoff` (#206) | opus | 46ec77a5 | The review overlay is read-only plus `e` for `$EDITOR` rather than an in-overlay editor; `clio-coder usage report` does not label handoff rounds. |
| fleet authoring CLI (#209) | sol | c2b6058e | |
| collapse `fleet.ts` onto `executeFleetRun` (refs #208, #209) | sol | a95d5ca9 | `executeFleetRun` now owns the durable fleet-run record and `planFleetResume`; TUI-started runs are resumable from the CLI. |
| `/fleet run` approval preview (#208) | opus | c2693c73 | `src/cli/fleet.ts` still runs its own copy of the scheduler loop; it must collapse onto `executeFleetRun` when the fleet authoring CLI merges. Promoting the run path to a `DispatchContract` method is deferred until a second caller needs it. |

## Wave 2

| Brief | Pane | Commit | Deferred |
| --- | --- | --- | --- |
| council contract | sol | pending | |
| council surface + `/council` | opus | pending | |
| fleet contract v5 (plan, gate, per-step target) | sol | pending | |
| oracle recipe + watchdog (#210) | opus | merged; hash recorded in the next row update | No evidence-domain turn diff exists; the watchdog coalesces per-call `edit`/`write`/`artifact` diffs. `oracle` is excluded from the compiled prompt roster (operator-only shadow). No Settings Center row for `watchdog`. |

## Wave 3

| Brief | Pane | Commit | Deferred |
| --- | --- | --- | --- |
| resource library | sol | pending | |

## Decisions made during implementation

- #155 reconciliation: the model-facing request gains one `intent` object
  (`read_roots`, `write_roots`, `relevant_paths`, `expected_outputs`,
  `verification: [{check, timeout_ms?}]`). `gate: "<check>"` is the only
  shorthand and expands to one verification entry. There is no separate
  top-level `verify` field. A `check` is a declared id resolved through the
  verify tool's `DeclaredCheck` projection at admission; model text never
  becomes argv. Verification executes on singular, parallel, sequential,
  pipeline, and detached runs in this cut; review and compete accept the
  intent shape but refuse verification entries with a named reason.
- Receipt integrity version moves 15 to 16 with the `verification` field.
