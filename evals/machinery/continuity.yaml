# Generated by evals/machinery/lib/suite-gen.ts from the suite table in
# evals/machinery/lib/suites.ts. Edit the table and regenerate; do not edit by hand.
version: 2
suite:
  id: machinery-continuity
  title: Machinery, continuity handoff transactions
  visibility: public
  description: self_compact handoff preparation, the summary and eviction checkpoints that reserve a commit before the durability barrier, and correlated continuation delivery.
matrix:
  dimensions: []
  targets:
    - id: default
  repeats: 1
tasks:
  - id: continuity.handoff-preparation
    tags: [machinery, machinery-only, continuity]
    workspace: &workspace {kind: local, path: ../..}
    runner: &runner {kind: external-command, commands: ["true"]}
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite continuity --scenario handoff-preparation
    metrics: &metrics {collect: [task.solved]}
    timeoutMs: &timeout 120000
  - id: continuity.summary-checkpoint
    tags: [machinery, machinery-only, continuity]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite continuity --scenario summary-checkpoint
    metrics: *metrics
    timeoutMs: *timeout
  - id: continuity.eviction-checkpoint
    tags: [machinery, machinery-only, continuity]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite continuity --scenario eviction-checkpoint
    metrics: *metrics
    timeoutMs: *timeout
  - id: continuity.continuation-delivery
    tags: [machinery, machinery-only, continuity]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite continuity --scenario continuation-delivery
    metrics: *metrics
    timeoutMs: *timeout
  - id: continuity.receipt-authority
    tags: [machinery, machinery-only, continuity]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite continuity --scenario receipt-authority
    metrics: *metrics
    timeoutMs: *timeout
  - id: continuity.barrier-failure
    tags: [machinery, machinery-only, continuity]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite continuity --scenario barrier-failure
    metrics: *metrics
    timeoutMs: *timeout
baseline:
  file: baselines/continuity.json
  pin: [task.solved, custom.digest.behavior]
