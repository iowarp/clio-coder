# Generated by evals/machinery/lib/suite-gen.ts from the suite table in
# evals/machinery/lib/suites.ts. Edit the table and regenerate; do not edit by hand.
version: 2
suite:
  id: machinery-prompt-compile
  title: Machinery, prompt compilation layering
  visibility: public
  description: Prompt layer ordering, the layout version its manifest records, and the capability and role guidance that renders only when the surface carries the tool it describes.
matrix:
  dimensions: []
  targets:
    - id: default
  repeats: 1
tasks:
  - id: prompt-compile.section-order-volatility
    tags: [machinery, machinery-only, prompt-compile]
    workspace: &workspace {kind: local, path: ../..}
    runner: &runner {kind: external-command, commands: ["true"]}
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite prompt-compile --scenario section-order-volatility
    metrics: &metrics {collect: [task.solved]}
    timeoutMs: &timeout 120000
  - id: prompt-compile.layout-version
    tags: [machinery, machinery-only, prompt-compile]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite prompt-compile --scenario layout-version
    metrics: *metrics
    timeoutMs: *timeout
  - id: prompt-compile.conditional-capability-guidance
    tags: [machinery, machinery-only, prompt-compile]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite prompt-compile --scenario conditional-capability-guidance
    metrics: *metrics
    timeoutMs: *timeout
  - id: prompt-compile.stable-prefix-invariance
    tags: [machinery, machinery-only, prompt-compile]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite prompt-compile --scenario stable-prefix-invariance
    metrics: *metrics
    timeoutMs: *timeout
  - id: prompt-compile.skill-activation-policy
    tags: [machinery, machinery-only, prompt-compile]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite prompt-compile --scenario skill-activation-policy
    metrics: *metrics
    timeoutMs: *timeout
  - id: prompt-compile.worker-prompt-layers
    tags: [machinery, machinery-only, prompt-compile]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite prompt-compile --scenario worker-prompt-layers
    metrics: *metrics
    timeoutMs: *timeout
baseline:
  file: baselines/prompt-compile.json
  pin: [task.solved, custom.digest.behavior]
