# Generated by evals/machinery/lib/suite-gen.ts from the suite table in
# evals/machinery/lib/suites.ts. Edit the table and regenerate; do not edit by hand.
version: 2
suite:
  id: machinery-context-budget
  title: Machinery, context budget enforcement
  visibility: public
  description: Input plus reserved output admission at submission, at the post-tool continuation guard, and at the provider boundary that re-derives the wire ceiling.
matrix:
  dimensions: []
  targets:
    - id: default
  repeats: 1
tasks:
  - id: context-budget.submission-admission
    tags: [machinery, machinery-only, context-budget]
    workspace: &workspace {kind: local, path: ../..}
    runner: &runner {kind: external-command, commands: ["true"]}
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite context-budget --scenario submission-admission
    metrics: &metrics {collect: [task.solved]}
    timeoutMs: &timeout 120000
  - id: context-budget.output-reserve-resolution
    tags: [machinery, machinery-only, context-budget]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite context-budget --scenario output-reserve-resolution
    metrics: *metrics
    timeoutMs: *timeout
  - id: context-budget.provider-boundary-ceiling
    tags: [machinery, machinery-only, context-budget]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite context-budget --scenario provider-boundary-ceiling
    metrics: *metrics
    timeoutMs: *timeout
  - id: context-budget.live-view-admission
    tags: [machinery, machinery-only, context-budget]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite context-budget --scenario live-view-admission
    metrics: *metrics
    timeoutMs: *timeout
  - id: context-budget.tool-continuation-guard
    tags: [machinery, machinery-only, context-budget]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite context-budget --scenario tool-continuation-guard
    metrics: *metrics
    timeoutMs: *timeout
  - id: context-budget.pressure-policy-resolution
    tags: [machinery, machinery-only, context-budget]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite context-budget --scenario pressure-policy-resolution
    metrics: *metrics
    timeoutMs: *timeout
baseline:
  file: baselines/context-budget.json
  pin: [task.solved, custom.digest.behavior]
