# Generated by evals/machinery/lib/suite-gen.ts from the suite table in
# evals/machinery/lib/suites.ts. Edit the table and regenerate; do not edit by hand.
version: 2
suite:
  id: machinery-dispatch-admission
  title: Machinery, dispatch admission authority
  visibility: public
  description: Worker permission ceilings, approval reuse scoping and deny/stop semantics, measured through the production admission and safety seams with a scripted worker.
matrix:
  dimensions: []
  targets:
    - id: default
  repeats: 1
tasks:
  - id: dispatch-admission.worker-autonomy-ceiling
    tags: [machinery, machinery-only, dispatch-admission]
    workspace: &workspace {kind: local, path: ../..}
    runner: &runner {kind: external-command, commands: ["true"]}
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite dispatch-admission --scenario worker-autonomy-ceiling
    metrics: &metrics {collect: [task.solved]}
    timeoutMs: &timeout 120000
  - id: dispatch-admission.autonomy-disposition-matrix
    tags: [machinery, machinery-only, dispatch-admission]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite dispatch-admission --scenario autonomy-disposition-matrix
    metrics: *metrics
    timeoutMs: *timeout
  - id: dispatch-admission.worker-scope-subset
    tags: [machinery, machinery-only, dispatch-admission]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite dispatch-admission --scenario worker-scope-subset
    metrics: *metrics
    timeoutMs: *timeout
  - id: dispatch-admission.approval-axis-scoping
    tags: [machinery, machinery-only, dispatch-admission]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite dispatch-admission --scenario approval-axis-scoping
    metrics: *metrics
    timeoutMs: *timeout
  - id: dispatch-admission.approval-reuse-scoping
    tags: [machinery, machinery-only, dispatch-admission]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite dispatch-admission --scenario approval-reuse-scoping
    metrics: *metrics
    timeoutMs: *timeout
  - id: dispatch-admission.deny-stop-semantics
    tags: [machinery, machinery-only, dispatch-admission]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite dispatch-admission --scenario deny-stop-semantics
    metrics: *metrics
    timeoutMs: *timeout
  - id: dispatch-admission.sealed-autonomy-enforcement
    tags: [machinery, machinery-only, dispatch-admission]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite dispatch-admission --scenario sealed-autonomy-enforcement
    metrics: *metrics
    timeoutMs: *timeout
baseline:
  file: baselines/dispatch-admission.json
  pin: [task.solved, custom.digest.behavior]
