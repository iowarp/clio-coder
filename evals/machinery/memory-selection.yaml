# Generated by evals/machinery/lib/suite-gen.ts from the suite table in
# evals/machinery/lib/suites.ts. Edit the table and regenerate; do not edit by hand.
version: 2
suite:
  id: machinery-memory-selection
  title: Machinery, durable memory selection
  visibility: public
  description: Durable memory selection frozen across one prepared turn and its continuations, re-read on a new attempt, and revoked when the bounded store cannot be read.
matrix:
  dimensions: []
  targets:
    - id: default
  repeats: 1
tasks:
  - id: memory-selection.frozen-within-turn
    tags: [machinery, machinery-only, memory-selection]
    workspace: &workspace {kind: local, path: ../..}
    runner: &runner {kind: external-command, commands: ["true"]}
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite memory-selection --scenario frozen-within-turn
    metrics: &metrics {collect: [task.solved]}
    timeoutMs: &timeout 120000
  - id: memory-selection.continuation-inherits-frame
    tags: [machinery, machinery-only, memory-selection]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite memory-selection --scenario continuation-inherits-frame
    metrics: *metrics
    timeoutMs: *timeout
  - id: memory-selection.authority-change-rereads
    tags: [machinery, machinery-only, memory-selection]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite memory-selection --scenario authority-change-rereads
    metrics: *metrics
    timeoutMs: *timeout
  - id: memory-selection.prewarm-never-freezes
    tags: [machinery, machinery-only, memory-selection]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite memory-selection --scenario prewarm-never-freezes
    metrics: *metrics
    timeoutMs: *timeout
  - id: memory-selection.selection-reuse-across-turns
    tags: [machinery, machinery-only, memory-selection]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite memory-selection --scenario selection-reuse-across-turns
    metrics: *metrics
    timeoutMs: *timeout
  - id: memory-selection.read-failure-revokes
    tags: [machinery, machinery-only, memory-selection]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/machinery/lib/driver.ts --suite memory-selection --scenario read-failure-revokes
    metrics: *metrics
    timeoutMs: *timeout
baseline:
  file: baselines/memory-selection.json
  pin: [task.solved, custom.digest.behavior]
