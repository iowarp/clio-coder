# Generated by evals/tool-bench/lib/suite-gen.ts from the scenario table in
# evals/tool-bench/lib/corpus.ts. Edit the table and regenerate; do not edit by hand.
version: 2
suite:
  id: tool-bench-find-holdout
  title: Tool bench, find tool, holdout split
  visibility: public
  description: One task per seeded find scenario (seed 1, split holdout), measured through invokeRegisteredTool.
matrix:
  dimensions: []
  targets:
    - id: default
  repeats: 1
tasks:
  - id: find.holdout.name-100
    tags: [tool-bench, find, holdout]
    workspace: &workspace {kind: temp-copy, path: ../.., excludes: [.claude-plugin, .github, .superpowers, CHANGELOG.md, apps, assets, benchmarks, docs, examples, library, patches, pnpm-lock.yaml, scripts, src, tests]}
    runner: &runner {kind: external-command, commands: [sh evals/tool-bench/lib/link-deps.sh]}
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.name-100 --seed 1 --split holdout
    metrics: &metrics {collect: [task.solved]}
    timeoutMs: 60000
  - id: find.holdout.extension-1k
    tags: [tool-bench, find, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.extension-1k --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: find.holdout.nested-depth-12
    tags: [tool-bench, find, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.nested-depth-12 --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: find.holdout.empty-result-1k
    tags: [tool-bench, find, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.empty-result-1k --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: find.holdout.hidden-files
    tags: [tool-bench, find, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.hidden-files --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: find.holdout.ignored-dirs
    tags: [tool-bench, find, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.ignored-dirs --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: find.holdout.dirs-and-files
    tags: [tool-bench, find, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.dirs-and-files --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: find.holdout.symlink-loop
    tags: [tool-bench, find, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.symlink-loop --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: find.holdout.symlink-dir-outside
    tags: [tool-bench, find, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.symlink-dir-outside --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: find.holdout.order-200
    tags: [tool-bench, find, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.order-200 --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: find.holdout.tree-10
    tags: [tool-bench, find, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.tree-10 --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: find.holdout.tree-10k
    tags: [tool-bench, find, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.tree-10k --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: find.holdout.err-symlink-outside-path
    tags: [tool-bench, find, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.err-symlink-outside-path --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: find.holdout.err-missing-root
    tags: [tool-bench, find, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.holdout.err-missing-root --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
baseline:
  file: baselines/find.holdout.json
  pin: [task.solved, custom.digest.behavior, custom.counters.fs_ops]
