# Generated by evals/tool-bench/lib/suite-gen.ts from the scenario table in
# evals/tool-bench/lib/corpus.ts. Edit the table and regenerate; do not edit by hand.
version: 2
suite:
  id: tool-bench-grep-search-full
  title: Tool bench, grep tool, search split, full profile with the 100 MB case
  visibility: public
  description: One task per seeded grep scenario (seed 1, split search), measured through invokeRegisteredTool.
matrix:
  dimensions: []
  targets:
    - id: default
  repeats: 1
tasks:
  - id: grep.search.literal-10
    tags: [tool-bench, grep, search]
    workspace: &workspace {kind: temp-copy, path: ../.., excludes: [.claude-plugin, .github, .superpowers, CHANGELOG.md, apps, assets, benchmarks, docs, examples, library, patches, pnpm-lock.yaml, scripts, src, tests]}
    runner: &runner {kind: external-command, commands: [sh evals/tool-bench/lib/link-deps.sh]}
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.literal-10 --seed 1 --split search
    metrics: &metrics {collect: [task.solved]}
    timeoutMs: 60000
  - id: grep.search.regex-100
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.regex-100 --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.ignore-case-100
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.ignore-case-100 --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.no-match-1k
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.no-match-1k --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.every-file-50
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.every-file-50 --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.huge-file-limit-16m
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.huge-file-limit-16m --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.huge-file-limit-100m
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.huge-file-limit-100m --seed 1 --split search
    metrics: *metrics
    timeoutMs: 600000
  - id: grep.search.byte-cap-4m
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.byte-cap-4m --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.binary-nul
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.binary-nul --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.invalid-utf8
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.invalid-utf8 --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.long-line-256k
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.long-line-256k --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.gitignore
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.gitignore --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.symlink-dir-inside
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.symlink-dir-inside --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.symlink-dir-outside
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.symlink-dir-outside --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.tree-10k
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.tree-10k --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.tree-50k
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.tree-50k --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.context-1k
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.context-1k --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.err-symlink-outside-path
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.err-symlink-outside-path --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.err-missing-path
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.err-missing-path --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.search.err-invalid-regex
    tags: [tool-bench, grep, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.search.err-invalid-regex --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
