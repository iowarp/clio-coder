# Generated by evals/tool-bench/lib/suite-gen.ts from the scenario table in
# evals/tool-bench/lib/corpus.ts. Edit the table and regenerate; do not edit by hand.
version: 2
suite:
  id: tool-bench-bash-holdout-full
  title: Tool bench, bash tool, holdout split, full profile with the 100 MB cases
  visibility: public
  description: One task per seeded bash scenario (seed 1, split holdout), measured through invokeRegisteredTool.
matrix:
  dimensions: []
  targets:
    - id: default
  repeats: 1
tasks:
  - id: bash.holdout.stdout-plain
    tags: [tool-bench, bash, holdout]
    workspace: &workspace {kind: temp-copy, path: ../.., excludes: [.claude-plugin, .github, .superpowers, CHANGELOG.md, apps, assets, benchmarks, docs, examples, library, patches, pnpm-lock.yaml, scripts, src, tests]}
    runner: &runner {kind: external-command, commands: [sh evals/tool-bench/lib/link-deps.sh]}
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.stdout-plain --seed 1 --split holdout
    metrics: &metrics {collect: [task.solved]}
    timeoutMs: 60000
  - id: bash.holdout.stderr-plain
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.stderr-plain --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.both-streams
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.both-streams --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.exit-42
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.exit-42 --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.exit-1
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.exit-1 --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.err-not-found
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.err-not-found --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.read-input-64k
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.read-input-64k --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.read-input-900k
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.read-input-900k --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.write-file
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.write-file --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.append-file
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.append-file --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.cwd-inside
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.cwd-inside --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.err-cwd-escape
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.err-cwd-escape --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.no-final-newline
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.no-final-newline --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.utf8-multibyte-16k
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.utf8-multibyte-16k --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.binary-output
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.binary-output --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.policy-bounded-4m
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.policy-bounded-4m --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.policy-summary-4m
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.policy-summary-4m --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.policy-metadata-only-4m
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.policy-metadata-only-4m --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.policy-full-1k
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.policy-full-1k --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.err-timeout
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.err-timeout --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.err-empty-command
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.err-empty-command --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.parked-at-auto-edit
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.parked-at-auto-edit --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.parked-read-only
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.parked-read-only --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.holdout.output-cap-20m
    tags: [tool-bench, bash, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.holdout.output-cap-20m --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
