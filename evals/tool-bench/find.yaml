# Generated by evals/tool-bench/lib/suite-gen.ts from the scenario table in
# evals/tool-bench/lib/corpus.ts. Edit the table and regenerate; do not edit by hand.
version: 2
suite:
  id: tool-bench-find-search
  title: Tool bench, find tool, search split
  visibility: public
  description: One task per seeded find scenario (seed 1, split search), measured through invokeRegisteredTool.
matrix:
  dimensions: []
  targets:
    - id: default
  repeats: 1
tasks:
  - id: find.search.name-100
    tags: [tool-bench, find, search]
    workspace: &workspace {kind: temp-copy, path: ../.., excludes: [.claude-plugin, .github, .superpowers, CHANGELOG.md, apps, assets, benchmarks, docs, examples, library, patches, pnpm-lock.yaml, scripts, src, tests]}
    runner: &runner {kind: external-command, commands: [sh evals/tool-bench/lib/link-deps.sh]}
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.name-100 --seed 1 --split search
    metrics: &metrics {collect: [task.solved]}
    timeoutMs: 60000
  - id: find.search.extension-1k
    tags: [tool-bench, find, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.extension-1k --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: find.search.nested-depth-12
    tags: [tool-bench, find, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.nested-depth-12 --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: find.search.empty-result-1k
    tags: [tool-bench, find, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.empty-result-1k --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: find.search.hidden-files
    tags: [tool-bench, find, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.hidden-files --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: find.search.ignored-dirs
    tags: [tool-bench, find, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.ignored-dirs --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: find.search.dirs-and-files
    tags: [tool-bench, find, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.dirs-and-files --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: find.search.symlink-loop
    tags: [tool-bench, find, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.symlink-loop --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: find.search.symlink-dir-outside
    tags: [tool-bench, find, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.symlink-dir-outside --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: find.search.order-200
    tags: [tool-bench, find, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.order-200 --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: find.search.tree-10
    tags: [tool-bench, find, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.tree-10 --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: find.search.tree-10k
    tags: [tool-bench, find, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.tree-10k --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: find.search.err-symlink-outside-path
    tags: [tool-bench, find, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.err-symlink-outside-path --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: find.search.err-missing-root
    tags: [tool-bench, find, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario find.search.err-missing-root --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
baseline:
  file: baselines/find.search.json
  pin: [task.solved, custom.digest.behavior, custom.counters.fs_ops]
