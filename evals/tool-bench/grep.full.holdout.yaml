# Generated by evals/tool-bench/lib/suite-gen.ts from the scenario table in
# evals/tool-bench/lib/corpus.ts. Edit the table and regenerate; do not edit by hand.
version: 2
suite:
  id: tool-bench-grep-holdout-full
  title: Tool bench, grep tool, holdout split, full profile with the 100 MB case
  visibility: public
  description: One task per seeded grep scenario (seed 1, split holdout), measured through invokeRegisteredTool.
matrix:
  dimensions: []
  targets:
    - id: default
  repeats: 1
tasks:
  - id: grep.holdout.literal-10
    tags: [tool-bench, grep, holdout]
    workspace: &workspace {kind: temp-copy, path: ../.., excludes: [.claude-plugin, .github, .superpowers, CHANGELOG.md, apps, assets, benchmarks, docs, examples, library, patches, pnpm-lock.yaml, scripts, src, tests]}
    runner: &runner {kind: external-command, commands: [sh evals/tool-bench/lib/link-deps.sh]}
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.literal-10 --seed 1 --split holdout
    metrics: &metrics {collect: [task.solved]}
    timeoutMs: 60000
  - id: grep.holdout.regex-100
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.regex-100 --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.ignore-case-100
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.ignore-case-100 --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.no-match-1k
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.no-match-1k --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.every-file-50
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.every-file-50 --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.huge-file-limit-16m
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.huge-file-limit-16m --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.huge-file-limit-100m
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.huge-file-limit-100m --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 600000
  - id: grep.holdout.byte-cap-4m
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.byte-cap-4m --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.binary-nul
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.binary-nul --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.invalid-utf8
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.invalid-utf8 --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.long-line-256k
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.long-line-256k --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.gitignore
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.gitignore --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.symlink-dir-inside
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.symlink-dir-inside --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.symlink-dir-outside
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.symlink-dir-outside --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.tree-10k
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.tree-10k --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.tree-50k
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.tree-50k --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.context-1k
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.context-1k --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.err-symlink-outside-path
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.err-symlink-outside-path --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.err-missing-path
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.err-missing-path --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
  - id: grep.holdout.err-invalid-regex
    tags: [tool-bench, grep, holdout]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario grep.holdout.err-invalid-regex --seed 1 --split holdout
    metrics: *metrics
    timeoutMs: 60000
