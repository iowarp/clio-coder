# Generated by evals/tool-bench/lib/suite-gen.ts from the scenario table in
# evals/tool-bench/lib/corpus.ts. Edit the table and regenerate; do not edit by hand.
version: 2
suite:
  id: tool-bench-bash-search
  title: Tool bench, bash tool, search split
  visibility: public
  description: One task per seeded bash scenario (seed 1, split search), measured through invokeRegisteredTool.
matrix:
  dimensions: []
  targets:
    - id: default
  repeats: 1
tasks:
  - id: bash.search.stdout-plain
    tags: [tool-bench, bash, search]
    workspace: &workspace {kind: temp-copy, path: ../.., excludes: [.claude-plugin, .github, .superpowers, CHANGELOG.md, apps, assets, benchmarks, docs, examples, library, patches, pnpm-lock.yaml, scripts, src, tests]}
    runner: &runner {kind: external-command, commands: [sh evals/tool-bench/lib/link-deps.sh]}
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.stdout-plain --seed 1 --split search
    metrics: &metrics {collect: [task.solved]}
    timeoutMs: 60000
  - id: bash.search.stderr-plain
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.stderr-plain --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.both-streams
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.both-streams --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.exit-42
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.exit-42 --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.exit-1
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.exit-1 --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.err-not-found
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.err-not-found --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.read-input-64k
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.read-input-64k --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.read-input-900k
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.read-input-900k --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.write-file
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.write-file --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.append-file
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.append-file --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.cwd-inside
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.cwd-inside --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.err-cwd-escape
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.err-cwd-escape --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.no-final-newline
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.no-final-newline --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.utf8-multibyte-16k
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.utf8-multibyte-16k --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.binary-output
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.binary-output --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.policy-bounded-4m
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.policy-bounded-4m --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.policy-summary-4m
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.policy-summary-4m --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.policy-metadata-only-4m
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.policy-metadata-only-4m --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.policy-full-1k
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.policy-full-1k --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.err-timeout
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.err-timeout --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.err-empty-command
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.err-empty-command --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.parked-at-auto-edit
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.parked-at-auto-edit --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
  - id: bash.search.parked-read-only
    tags: [tool-bench, bash, search]
    workspace: *workspace
    runner: *runner
    verify:
      measure:
        - node --import tsx evals/tool-bench/lib/driver.ts --scenario bash.search.parked-read-only --seed 1 --split search
    metrics: *metrics
    timeoutMs: 60000
baseline:
  file: baselines/bash.search.json
  pin: [task.solved, custom.digest.behavior, custom.counters.fs_ops]
